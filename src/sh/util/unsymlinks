#!/usr/bin/env sh

. io.sh
. shellverbose.sh

for ln; do
    [ -L "$ln" ] || {
        std_logerr "no such symlink: $ln"
        continue
    }
    realpath="$(realpath -- "$ln")"
    rm -f -- "$ln"
    ln -sf -- "$realpath" "$ln"
done
