#!/usr/bin/env sh
# Usage: mvloc location file1 ... fileN
# Equivalent to: mv file1 ... fileN "$(getdirsafe location)"

dir="$(getdirsafe "$1")" || {
    printf "[ERROR]: getdirsafe failed, aborting.\n"
    exit 1
}

shift
set -- "$@" "$dir"
mv -n -- "$@"
