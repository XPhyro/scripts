#!/usr/bin/env sh
# Toggle X keymap.

. shellverbose.sh

setkm() {
    setxkbmap "$1"
    setxkb "$1"
}

km="$(xkb-switch)"
setkm "$(printf "%s\n" "$km" | eval "END= DEF=gb fmaps ${KMCYCLE_MAPPINGS:-gb=tr}")"
