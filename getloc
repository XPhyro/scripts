#!/usr/bin/env sh

loc="$HOME/documents/sys-admin/locations"

[ "$@" ] || exit

for i in "$@"
do
    [ "$i" = "*" ] && {
        awk '{print $2}' "$loc"
        exit
    }
done

if ! [ "$2" ]
then
    expandpath "$( grep -E "^$1\s+.*" "$loc" | awk '{print $2}' )"
    exit
fi

for i in "$@"
do
    expandpath "$( printf "%s\n" "$( grep -E "^$i\s+.*" "$loc" | awk '{print $2}' )" )"
done
